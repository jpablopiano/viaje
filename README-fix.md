# Fix rutas imagenes GitHub Pages

Cambios aplicados:
- Todas las imagenes quedaron apuntando a `./img/*.jpg`.
- Los links internos quedaron como `./index.html`, `./italia.html`, `./usa.html`.
- No se cambiaron los nombres ni extensiones de las imagenes.
- No hay referencias a archivos `.svg` externos; los `<svg>` que quedan son iconos inline de flechas.

Para publicar:
1. Reemplazar estos 3 HTML en la raiz del repo.
2. Confirmar que la carpeta `img` este en la raiz del repo.
3. Confirmar que los nombres coincidan exacto, respetando minusculas/mayusculas.
